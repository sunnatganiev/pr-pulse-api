# Application
PORT=3000
NODE_ENV=development
CORS_ORIGIN=http://localhost:5173

# Database (PostgreSQL 16)
DB_HOST=localhost
DB_PORT=5432
DB_USER=pr_pulse
DB_PASSWORD=pr_pulse
DB_NAME=pr_pulse

GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=
JWT_SECRET=
JWT_EXPIRES_IN=7d
COOKIE_NAME=prpulse_jwt
WEB_URL=http://localhost:5173
API_URL=http://localhost:3000

# GitHub integration (Session 3)
# Development-only Personal Access Token (suboptimal; Session 5 -> GitHub MCP).
# Scopes: repo (private repos) yoki public_repo (faqat public).
GITHUB_TOKEN_DEV=